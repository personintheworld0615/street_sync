# Copy to assets/.env and fill in values.
# Get LAN IP: ipconfig getifaddr en0
DEV_HOST=localhost
API_PORT=8001

# Optional production API override
# CLOUD_URL=https://street-sync.onrender.com

# Gemini — used for voice-report title / severity / category
# Get a key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
# Optional model override (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# Supabase Auth — Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Google Sign-In (native) — required for iPhone
# 1) Create an iOS OAuth client (bundle com.example.streetSync) in Google Cloud Console
# 2) Also put the same iOS client id + URL scheme in ios/Runner/Secrets.xcconfig
GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com
# Web / "server" client ID — required so Supabase gets an ID token
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
